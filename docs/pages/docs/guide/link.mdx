# Next.js Link 组件

所有相对的 Markdown 链接都会自动转换为 Next.js 链接。这意味着目标页面将会被预加载。当您点击链接时，页面将在客户端加载，就像一个单页应用（SPA）一样，而不会进行完整的页面加载。例如：

```md
点击[这里](/about)了解更多。
```

将等同于：

```jsx
import Link from 'next/link'

点击<Link href="/about">这里</Link>了解更多。
```

此功能使得在 Nextra 页面之间的导航快速且无缝衔接。


